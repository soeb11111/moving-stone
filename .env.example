# Password for the /admin studio. Set this in Vercel → Settings → Environment
# Variables, and locally in a .env file (never committed). See CMS.md.
ADMIN_PASSWORD=

# Set automatically when you connect a Vercel Blob store. Enables the Publish
# button; the editor works without it via Export + commit.
# BLOB_READ_WRITE_TOKEN=
